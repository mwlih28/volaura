# NeonDB Postgres connection string
DATABASE_URL=postgresql://USER:PASS@HOST/DB?sslmode=require

# SendGrid
SENDGRID_API_KEY=SG.xxxxx
SENDGRID_FROM=noreply@volaura.qzz.io
SENDGRID_FROM_NAME=VoLaura

# Public base URL used in verification & password reset email links.
# Must point to this server with SSL (uses cert on port 8444 currently).
BASE_URL=https://volaura.qzz.io:8444

# Ports
WS_PORT=8443
WSS_PORT=8444
